import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 10,
    margin: 10,
    // shadowOpacity: 1,
    // shadowRadius: 15,
  },
  input: {
    paddingHorizontal: 50,
  },
  underline: {
    borderWidth: 0.4,
    borderColor: 'gray',
  },
});
